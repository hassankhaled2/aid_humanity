import 'package:flutter/material.dart';
class DataEntryWidgets extends StatelessWidget {
  const DataEntryWidgets({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}